\NeedsTeXFormat{LaTeX2e}


\ProvidesPackage{master_on_dust}[15/03/2019 Custom Latex thesis style]

\RequirePackage[final]{microtype}

%% Encoding
\RequirePackage[T1]{fontenc}
\RequirePackage{booktabs}

\RequirePackage{subfiles}
\RequirePackage{algorithm2e}
\RequirePackage{rotating}
\RequirePackage{mathpazo}
           %Better referencing to figures etc.
\RequirePackage[nottoc]{tocbibind}
\RequirePackage{csquotes}
\RequirePackage{siunitx}
\RequirePackage[usenames,dvipsnames,svgnames,table,xcdraw]{xcolor}
\RequirePackage{chemformula}
\RequirePackage[font=it]{caption}
\RequirePackage{subcaption}
\RequirePackage{gensymb}
\RequirePackage{float}
\RequirePackage{eso-pic}
\RequirePackage{physics}
\RequirePackage{algpseudocode}
\RequirePackage{listings} 
\RequirePackage[acronym, toc]{glossaries}

\RequirePackage[toc,page]{appendix}
%Bibliography

\RequirePackage[backend      = biber,
                sortcites    = true,
                giveninits   = true,
                uniquename   = false,
                doi          = true,
                isbn         = true,
                url          = false,
                maxcitenames = 2,
                maxbibnames  = 10,
                uniquelist   = false,
                style        = authoryear]{biblatex}
\addbibresource{references.bib}
%Figures
\RequirePackage{graphicx}     
\RequirePackage{keyval,xifthen}

\graphicspath{{texfiles/figs}}
\RequirePackage{subcaption}   

%% Mathematics
\RequirePackage{amssymb}    % Extra symbols
\RequirePackage{mathtools}  % Fonts and environments for mathematical formulae

%% Initiate new paragraphs with a blank line
%% and no indentation
\setlength{\parindent}{0pt}
\setlength{\parskip}{1.5ex plus 0.5ex minus 0.2ex}


%% Relax parameters for line breaks
\pretolerance = 2000
\tolerance    = 6000
\hbadness     = 6000
\setlength{\emergencystretch}{3em}

\usepackage{titleps}
\renewpagestyle{plain}{%
\sethead{}{}{}
\setfoot[\thepage][][]{}{}{\thepage}
}%
\pagestyle{plain}

\RequirePackage[makeroom]{cancel}
\RequirePackage[left=3cm,%
                right=3cm,%
                top=3.00cm,%
                bottom=3.00cm,%
                headheight=12pt,%
                a4paper]{geometry}%
                
\RequirePackage[labelfont={bf,sf},%
                labelsep=period,%
                justification=raggedright]{caption}

\RequirePackage{xspace}         % Clever space
\RequirePackage{textcomp}       % Extra symbols

\RequirePackage{varioref}
\RequirePackage[pdfusetitle]{hyperref}
\RequirePackage[nameinlink, capitalize, noabbrev]{cleveref}

\RequirePackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\fancyfoot[RE,LO]{\thepage}
\fancyhead[LO]{\rightmark}
\fancyhead[LE]{\leftmark}


% \fancypagestyle{toc}{%
% \fancyhf{}%
% \fancyhead[RO]{\thepage}%
% \fancyhead[RO]{\textsl{TABLE OF CONTENTS}}%
% \fancyfoot[C]{\thepage}
% }
% \fancypagestyle{listoffigs}{%
% \fancyhf{}%
% \fancyhead[RO]{\thepage}%
% \fancyhead[RO]{\textsl{LIST OF FIGURES}}%
% \fancyfoot[C]{\thepage}
% }


\RequirePackage[obeyFinal,colorinlistoftodos,
                color       = LightGray,
                bordercolor = LightGray,
                textsize    = footnotesize,
                figwidth    = 0.99\linewidth]{todonotes} % Marginal notes
\RequirePackage{comment}                                 % Multiline comments
\RequirePackage[notref, notcite]{showkeys}               % Labels in margins
\renewcommand{\showkeyslabelformat}[1]
{
    \fbox
    {
        \parbox[t]{0.6\marginparwidth}
        {
            \centering\normalfont\footnotesize\ttfamily
            \path{#1}
        }
    }
}

